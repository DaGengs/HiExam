<?xml version="1.0" encoding="UTF-8"?>
<!--约束文件 --><!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.iruier.dao.TestDetailMapper">
   <insert id="save" parameterType="TestDetail">
       insert into t_testdetail(test_id, ques_id, score, user_no, answer, createTime) values(#{test_id}, #{ques_id}, #{score}, #{user_no}, #{answer}, #{createTime})
   </insert>
</mapper>